What is it?
===========

A fish plugin for the Xfce panel.


Installation
============

Run the following:

./autogen.sh
./configure <install settings>
make
make install

Example install settings:

64bit Ubuntu: --prefix=/usr --libdir=/usr/lib/x86_64-linux-gnu/
64bit Arch: --prefix=/usr --sysconfdir=/etc --libexecdir=/usr/lib --localstatedir=/var --disable-static